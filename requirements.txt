# ============================================================
# Ikaris Assistant — requirements.txt (v1.2.0)
# ============================================================
# Install:  pip install -r requirements.txt
# Note   :  CUDA-specific wheels (torch, faiss-gpu) require a
#           compatible NVIDIA driver. Swap for CPU variants if
#           running without a GPU.
# ============================================================

# ---- Core LLM / LangChain -----------------------------------
langchain-openai
langchain-core
langgraph

# ---- Hydra Configuration ------------------------------------
hydra-core>=1.3
omegaconf>=2.3

# ---- Embeddings / RAG ---------------------------------------
sentence-transformers
faiss-gpu                   # Use 'faiss-cpu' if no NVIDIA GPU
transformers

# ---- PubMed Biomedical Search --------------------------------
metapub

# ---- PDF Processing -----------------------------------------
PyPDF2

# ---- Audio (Sherpa-ONNX) ------------------------------------
sherpa-onnx                 # NPU/CUDA/CPU STT + TTS
sounddevice                 # Mic & speaker I/O
numpy

# ---- GUI -----------------------------------------------------
PyQt5

# ---- Hardware Monitoring -------------------------------------
psutil>=5.6.6
nvitop

# ---- Utilities -----------------------------------------------
python-dotenv
requests
tqdm
rich

# ---- Deep Learning Backend -----------------------------------
torch                       # GPU: pip install torch --index-url ...

# ---- Testing -------------------------------------------------
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
